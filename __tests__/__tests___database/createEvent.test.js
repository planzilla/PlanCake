import { get, post, patch } from '../server/controller';
import { CreateEvent } from '../client/src/components/body';


test('Should add event to Event model', () => {
  const title = 'Testing title';
  const location = 'Seattle, WA';

  
})